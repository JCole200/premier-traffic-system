# Database
# For local dev: file:./dev.db
# For production (Vercel): postgresql://... or similar
DATABASE_URL="file:./dev.db"

# Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=password
SMTP_FROM=noreply@premier.org.uk
